local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")
local rootPart = character:WaitForChild("HumanoidRootPart")

-- UI Setup
local screenGui = Instance.new("ScreenGui", player.PlayerGui)
screenGui.Name = "NuclearBrainrotUI"
screenGui.ResetOnSpawn = false

-- Main Frame
local frame = Instance.new("Frame", screenGui)
frame.Size = UDim2.new(0, 260, 0, 500)
frame.Position = UDim2.new(0.5, -130, 0.2, 0)
frame.BackgroundColor3 = Color3.fromRGB(5, 5, 5)
frame.BorderSizePixel = 0
frame.Active = true
frame.Draggable = true
Instance.new("UICorner", frame)

local title = Instance.new("TextLabel", frame)
title.Size = UDim2.new(1, 0, 0, 40)
title.BackgroundColor3 = Color3.fromRGB(255, 215, 0)
title.Text = "STUD BRAINROT V5"
title.TextColor3 = Color3.new(0,0,0)
title.Font = Enum.Font.FredokaOne
title.TextSize = 20
Instance.new("UICorner", title)

-- Variables
local flySpeed = 100
local flying = false
local banishSharks = false
local bv, bg

-- THE FIX: Remote Scanner Logic
local function tryTrigger(keywords)
    local found = 0
    for _, v in pairs(game:GetDescendants()) do
        if v:IsA("RemoteEvent") or v:IsA("RemoteFunction") then
            for _, key in pairs(keywords) do
                if v.Name:lower():find(key:lower()) then
                    pcall(function()
                        if v:IsA("RemoteEvent") then
                            v:FireServer()
                            v:FireServer(true)
                            v:FireServer(1)
                        else
                            v:InvokeServer()
                        end
                    end)
                    found = found + 1
                end
            end
        end
    end
    print("Triggered " .. found .. " remotes for keywords: " .. table.concat(keywords, ", "))
end

-- Button Helper
local function createBtn(text, pos, color, callback)
    local btn = Instance.new("TextButton", frame)
    btn.Size = UDim2.new(0.9, 0, 0, 35)
    btn.Position = pos
    btn.BackgroundColor3 = color
    btn.Text = text
    btn.Font = Enum.Font.SourceSansBold
    btn.TextColor3 = Color3.new(1,1,1)
    btn.TextSize = 14
    Instance.new("UICorner", btn)
    btn.MouseButton1Click:Connect(callback)
    return btn
end

-- 1. GIVE SPINS (Scans for all reward/spin events)
createBtn("SPAM GIVE SPINS", UDim2.new(0.05, 0, 0.1, 0), Color3.fromRGB(0, 150, 0), function()
    tryTrigger({"spin", "reward", "daily", "wheel", "luck"})
end)

-- 2. SPAWN CELESTIALS / BRAINROTS
createBtn("FORCE SPAWN CELESTIAL", UDim2.new(0.05, 0, 0.18, 0), Color3.fromRGB(200, 100, 0), function()
    tryTrigger({"spawn", "celestial", "summon", "brainrot", "boss"})
    -- Secret Code Entry (Sometimes works for Celestials)
    local codeRemote = game:GetService("ReplicatedStorage"):FindFirstChild("CodeRemote") or game:GetService("ReplicatedStorage"):FindFirstChild("Codes")
    if codeRemote then pcall(function() codeRemote:FireServer("EGG") end) end
end)

-- 3. BANISH SHARKS (Better Logic)
createBtn("BANISH SHARKS: OFF", UDim2.new(0.05, 0, 0.26, 0), Color3.fromRGB(0, 0, 150), function(btn)
    banishSharks = not banishSharks
    btn.Text = "BANISH SHARKS: " .. (banishSharks and "ON" or "OFF")
end)

task.spawn(function()
    while task.wait(0.3) do
        if banishSharks then
            for _, v in pairs(workspace:GetDescendants()) do
                if v:IsA("Model") and v.Name:lower():find("shark") then
                    v:MoveTo(Vector3.new(0, -500, 0)) -- Sends them to the shadow realm
                end
            end
        end
    end
end)

-- 4. FLY (F)
createBtn("FLY (F)", UDim2.new(0.05, 0, 0.34, 0), Color3.fromRGB(50, 50, 50), function()
    flying = not flying
    if flying then
        humanoid.PlatformStand = true
        bv = Instance.new("BodyVelocity", rootPart)
        bv.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
        bg = Instance.new("BodyGyro", rootPart)
        bg.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
    else
        if bv then bv:Destroy() end
        if bg then bg:Destroy() end
        humanoid.PlatformStand = false
    end
end)

-- Slider Helper
local function createSlider(name, pos, min, max, def, cb)
    local lab = Instance.new("TextLabel", frame)
    lab.Size = UDim2.new(0.9, 0, 0, 20); lab.Position = pos; lab.Text = name .. ": " .. def; lab.TextColor3 = Color3.new(1,1,1); lab.BackgroundTransparency = 1; lab.Font = Enum.Font.SourceSansBold
    local trk = Instance.new("Frame", frame); trk.Size = UDim2.new(0.9, 0, 0, 6); trk.Position = pos + UDim2.new(0,0,0,22); trk.BackgroundColor3 = Color3.fromRGB(40,40,40)
    local thm = Instance.new("TextButton", trk); thm.Size = UDim2.new(0, 14, 0, 14); thm.Position = UDim2.new(0,0,0.5,-7); thm.BackgroundColor3 = Color3.fromRGB(255,215,0); thm.Text = ""
    local drag = false
    thm.MouseButton1Down:Connect(function() drag = true end)
    UserInputService.InputEnded:Connect(function(i) if i.UserInputType == Enum.UserInputType.MouseButton1 then drag = false end end)
    RunService.RenderStepped:Connect(function()
        if drag then
            local rel = math.clamp((UserInputService:GetMouseLocation().X - trk.AbsolutePosition.X) / trk.AbsoluteSize.X, 0, 1)
            thm.Position = UDim2.new(rel, -7, 0.5, -7)
            local val = math.floor(min + (rel * (max - min)))
            lab.Text = name .. ": " .. val
            cb(val)
        end
    end)
end

createSlider("Boat/Walk Speed", UDim2.new(0.05, 0, 0.45, 0), 16, 2500, 16, function(v) humanoid.WalkSpeed = v end)
createSlider("Fly Speed", UDim2.new(0.05, 0, 0.60, 0), 10, 1000, 100, function(v) flySpeed = v end)

-- Fly Engine
RunService.RenderStepped:Connect(function()
    if flying and bv and bg then
        local cam = workspace.CurrentCamera.CFrame
        local dir = Vector3.new(0,0,0)
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then dir += cam.LookVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then dir -= cam.LookVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then dir -= cam.RightVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then dir += cam.RightVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then dir += Vector3.new(0,1,0) end
        bg.CFrame = cam
        bv.Velocity = dir * flySpeed
    end
end)

-- Hotkeys
UserInputService.InputBegan:Connect(function(i, g)
    if not g and i.KeyCode == Enum.KeyCode.RightAlt then frame.Visible = not frame.Visible end
end)
