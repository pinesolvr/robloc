local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local rootPart = character:WaitForChild("HumanoidRootPart")
local humanoid = character:WaitForChild("Humanoid")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")

-- [[ GLOBAL STATES ]] --
_G.GodMode = false
_G.AutoTP = false
_G.Noclip = false
_G.Flying = false
local flySpeed = 75
local baseCFrame = rootPart.CFrame

-- [[ 1. THE "TRUE" GOD MODE (Metatable Hook) ]] --
local mt = getrawmetatable(game)
local oldNamecall = mt.__namecall
setreadonly(mt, false)
mt.__namecall = newcclosure(function(self, ...)
    local method = getnamecallmethod()
    if _G.GodMode and method == "TakeDamage" then return nil end
    return oldNamecall(self, ...)
end)
setreadonly(mt, true)

-- [[ 2. AUTO-TP COLLECTOR ]] --
task.spawn(function()
    while task.wait(0.6) do
        if _G.AutoTP then
            for _, v in pairs(workspace:GetDescendants()) do
                if (v.Name:find("Block") or v.Name:find("Rot") or v.Name:find("Lucky")) then
                    local target = v:IsA("BasePart") and v or v:FindFirstChildWhichIsA("BasePart")
                    if target then
                        local oldPos = rootPart.CFrame
                        rootPart.CFrame = target.CFrame
                        firetouchinterest(rootPart, target, 0)
                        task.wait(0.1)
                        firetouchinterest(rootPart, target, 1)
                        rootPart.CFrame = oldPos
                    end
                end
            end
        end
    end
end)

-- [[ 3. FLY & NOCLIP HANDLERS ]] --
local function toggleFly()
    _G.Flying = not _G.Flying
    if _G.Flying then
        local bv = Instance.new("BodyVelocity", rootPart)
        bv.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
        bv.Velocity = Vector3.new(0,0.1,0)
        bv.Name = "StudFlight"
        task.spawn(function()
            while _G.Flying do
                local cam = workspace.CurrentCamera.CFrame
                local dir = Vector3.new(0,0,0)
                if UserInputService:IsKeyDown(Enum.KeyCode.W) then dir = dir + cam.LookVector end
                if UserInputService:IsKeyDown(Enum.KeyCode.S) then dir = dir - cam.LookVector end
                if UserInputService:IsKeyDown(Enum.KeyCode.A) then dir = dir - cam.RightVector end
                if UserInputService:IsKeyDown(Enum.KeyCode.D) then dir = dir + cam.RightVector end
                bv.Velocity = dir * flySpeed
                task.wait()
            end
            if rootPart:FindFirstChild("StudFlight") then rootPart.StudFlight:Destroy() end
        end)
    end
end

RunService.Stepped:Connect(function()
    if _G.Noclip then
        for _, v in pairs(character:GetDescendants()) do
            if v:IsA("BasePart") then v.CanCollide = false end
        end
    end
end)

-- [[ 4. MAIN UI CONSTRUCTION ]] --
local screenGui = Instance.new("ScreenGui", player.PlayerGui)
local frame = Instance.new("Frame", screenGui)
frame.Size = UDim2.new(0, 320, 0, 580)
frame.Position = UDim2.new(0.5, -160, 0.1, 0)
frame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
frame.BorderSizePixel = 4
frame.BorderColor3 = Color3.fromRGB(255, 215, 0)
frame.Active = true
frame.Draggable = true

local title = Instance.new("TextLabel", frame)
title.Size = UDim2.new(1, 0, 0, 40)
title.BackgroundColor3 = Color3.fromRGB(255, 215, 0)
title.Text = "STUD STUDIO MASTER"
title.Font = Enum.Font.FredokaOne
title.TextSize = 20

local scroll = Instance.new("ScrollingFrame", frame)
scroll.Size = UDim2.new(1, -10, 1, -50)
scroll.Position = UDim2.new(0, 5, 0, 45)
scroll.BackgroundTransparency = 1
scroll.CanvasSize = UDim2.new(0, 0, 2, 0)
Instance.new("UIListLayout", scroll).Padding = UDim.new(0, 5)

local function btn(txt, color, func)
    local b = Instance.new("TextButton", scroll)
    b.Size = UDim2.new(0.95, 0, 0, 35)
    b.BackgroundColor3 = color
    b.Text = txt
    b.TextColor3 = Color3.new(1,1,1)
    b.Font = Enum.Font.SourceSansBold
    b.MouseButton1Click:Connect(func)
    Instance.new("UICorner", b)
end

-- [[ BUTTON ASSIGNMENTS ]] --
btn("AUTO TP-COLLECT (TOGGLE)", Color3.fromRGB(150, 0, 255), function() _G.AutoTP = not _G.AutoTP end)
btn("GOD MODE (ON/OFF)", Color3.fromRGB(0, 150, 0), function() _G.GodMode = not _G.GodMode end)
btn("FLY: TOGGLE (F)", Color3.fromRGB(200, 0, 0), toggleFly)
btn("NOCLIP: TOGGLE", Color3.fromRGB(100, 100, 100), function() _G.Noclip = not _G.Noclip end)
btn("999 SPINS (REMOTE SPAM)", Color3.fromRGB(255, 150, 0), function()
    local r = game:GetService("ReplicatedStorage"):FindFirstChild("Spin", true)
    if r then for i=1, 50 do r:FireServer() r:InvokeServer() end end
end)
btn("FREE JAIL SHARK (FORCE)", Color3.fromRGB(0, 50, 200), function()
    local r = game:GetService("ReplicatedStorage"):FindFirstChild("JailShark", true)
    if r then r:FireServer("Spawn", true) end
end)
btn("DISABLE ALL SHARKS", Color3.fromRGB(200, 0, 50), function()
    for _, v in pairs(workspace:GetDescendants()) do
        if v.Name:lower():find("shark") and v:IsA("Model") then v:MoveTo(Vector3.new(0, -1000, 0)) end
    end
end)
btn("STUDIO HIERARCHY (F9)", Color3.fromRGB(50, 50, 50), function()
    print("--- HIERARCHY ---")
    for _, child in pairs(game:GetChildren()) do print(child.ClassName .. ": " .. child.Name) end
end)
btn("TP TO BASE", Color3.fromRGB(70, 70, 70), function() rootPart.CFrame = baseCFrame end)
btn("SET BASE HERE", Color3.fromRGB(40, 40, 40), function() baseCFrame = rootPart.CFrame end)

-- [[ KEYBINDS ]] --
UserInputService.InputBegan:Connect(function(i, g)
    if g then return end
    if i.KeyCode == Enum.KeyCode.F then toggleFly() end
    if i.KeyCode == Enum.KeyCode.RightAlt then frame.Visible = not frame.Visible end
end)

print("STUD MASTER LOADED - Press RightAlt to hide UI")
